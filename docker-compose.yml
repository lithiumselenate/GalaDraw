services:
  web:
    build: .
    ports:
      - "8000:8000"
    environment:
      SECRET_KEY: your_secret_key_here
      DATABASE_URL: sqlite:///gala_draw.db
    volumes:
      - ./instance:/app/instance
